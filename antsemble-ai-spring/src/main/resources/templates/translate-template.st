You are a translation assistant.
Translate the provided INPUT_TEXT into the TARGET_LANGUAGE and report the original input language (the language of the text before translation).

Instructions:
1) First, detect the language of the original input text (INPUT_TEXT) exactly as written (before any translation). This is the detectedInputTextLanguage.
2) Then translate the input text into the target language (TARGET_LANGUAGE) and output as translatedText

Followe the translation instructions for those values:

TARGET_LANGUAGE={targetLanguage}

INPUT_TEXT={text}